'''
==============================================================================
ШПАРАЛКА ПО СПИСКАМ (LIST) И СЛОВАРЯМ (DICT) ДЛЯ LEETCODE
==============================================================================
------------------------------------------------------------------------------
1. СПИСКИ (LIST)
------------------------------------------------------------------------------
Создание
arr = [10, 20, 30, 40]
Доступ по индексу (O(1))
first = arr[0]   # 10 (первый элемент) last = arr[-1]   # 40 (последний элемент)
Срезы (O(K))
sub = arr[1:3]   # [20, 30] (подмассив с индекса 1 до 2)
Добавление элементов
arr.append(50)         # [10, 20, 30, 40, 50] — добавить в конец (O(1)) arr.insert(0, 5)       # [5, 10, 20, 30, 40, 50] — вставить по индексу (O(N)) arr.extend([60, 70])   # объединить со списком (O(K))
Поиск элементов
if 20 in arr:          # Проверка наличия элемента (O(N)) idx = arr.index(20) # Получить индекс первого совпадения (O(N))
Перебор элементов с индексами (O(N))
for i, val in enumerate(arr): print(f"Индекс {i}: значение {val}")
------------------------------------------------------------------------------
2. СЛОВАРИ (DICT)
------------------------------------------------------------------------------
Создание
d = {"a": 1, "b": 2}
Прямое чтение (O(1)) — вызовет KeyError, если ключа нет
val = d["a"]
Безопасное чтение (O(1)) — вернет 0, если ключа "c" нет
val_safe = d.get("c", 0)
Запись и обновление (O(1))
d["c"] = 3             # Добавит новый ключ "c" со значением 3 d["a"] = 10            # Перезапишет значение для ключа "a" d.update({"d": 4, "e": 5}) # Объединение словарей
ПАТТЕРН: Подсчет частоты элементов / Frequency Map (O(1))
counts = {} for char in "banana": counts[char] = counts.get(char, 0) + 1  # Увеличит счетчик или создаст с 1
Поиск и проверка ключей (O(1))
if "a" in d: print("Ключ 'a' существует в словаре")
Перебор всех пар ключ-значение (O(N))
for key, value in d.items(): print(f"Ключ: {key}, Значение: {value}")
Поиск ключа по значению (O(N))
arget_val = 10 found_key = None for k, v in d.items(): if v == target_val: found_key = k break
'''